# 内测期真机联调：指向这台 Mac 的局域网地址，手机必须与 Mac 处于同一 WiFi。
# 换网络或 DHCP 续租导致 IP 变化时，改这里并重新执行 npm run build:ios:staging。
# 备案拿到正式域名后改用 .env.capacitor（https），并删除 Info.plist 的 NSAppTransportSecurity 整段。
VITE_API_BASE_URL=http://172.25.106.65:5237
